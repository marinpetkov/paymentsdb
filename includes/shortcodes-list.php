<?php
/**
 * Optional explicit list. Leave empty to let the plugin generate the list from sql-map.php.
 * If you want to hard-code a subset, return an array of tags like:
 * return ['incoming_bisera_amount_total', 'incoming_bisera_count_total', ...];
 */
return [];
